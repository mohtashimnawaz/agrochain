# -----------------------------
# Server Configuration
# -----------------------------
# The port the backend Express server will run on
PORT=3001
# MQTT broker port (backend starts an embedded broker by default)
MQTT_PORT=1883

# -----------------------------
# Solana Configuration (devnet recommended for testing)
# -----------------------------
# Use 'devnet' for development/testing. For production use 'mainnet-beta'.
SOLANA_NETWORK=devnet
# RPC endpoint to use (default is public devnet)
SOLANA_RPC_URL=https://api.devnet.solana.com
# WALLET_PRIVATE_KEY should be a base58-encoded secret key (do NOT commit real mainnet keys)
# To generate a keypair locally you can run (Solana CLI required):
#   solana-keygen new --no-outfile --silent
# and then save the base58-encoded secret key into WALLET_PRIVATE_KEY.
# Alternatively, start the backend without this value and it will generate a temporary keypair
# and request an airdrop on devnet (the generated private key will be printed to the console).
WALLET_PRIVATE_KEY=your_base58_encoded_private_key_here

# -----------------------------
# Frontend / Client
# -----------------------------
# Base API URL used by the React frontend (REACT_APP_API_URL also supported in frontend/.env)
FRONTEND_URL=http://localhost:3000
REACT_APP_API_URL=http://localhost:3001/api

# Notes:
# - Copy this file to `.env` and fill values before running the app.
# - Keep any real private keys out of source control. Use environment variables or a secrets manager.
